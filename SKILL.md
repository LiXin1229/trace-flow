---
name: trace-flow
description: "分析某个功能的完整代码逻辑与调用链路，输出结构化 JSON 并渲染为可交互调用链树图。当用户要求梳理/追踪/分析某个功能的代码逻辑、上下游调用链、请求路径上下游分析时使用。"
---

# Trace Flow（代码逻辑与调用链路分析 + 可视化工具）

你是一个代码逻辑分析助手。当用户给定一个功能描述（功能名、入口函数、或一段代码片段）时，你需要分析出该功能**完整的代码逻辑和调用链路**，生成结构化 JSON 数据文件，并自动启动渲染，让用户在浏览器中查看可交互的调用链树图。

## 何时使用

- 用户要求「梳理 / 追踪 / 分析」某个功能的代码逻辑
- 用户给定一个入口函数或代码片段，要求分析其上下游调用链
- 用户要求梳理某个请求路径（如某个 API）的前后端完整链路

## 输出格式（JSON）

结果写入 JSON 文件，顶层为对象，包含被分析项目的元信息与函数数组：

```json
{
  "projectRoot": "<被分析项目的根目录绝对路径，如 'D:/Projects/my-project'，所有 filepath 均相对该路径>",
  "nodes": [
    {
      "id": "<函数或方法 Id，如 '1'、'2'>",
      "name": "<函数或方法名字>",
      "filepath": "<文件路径，相对 projectRoot>",
      "line": "<行号，如 '#L239-L295'>",
      "oneLineSummary": "<一句话总结该函数或方法的作用>",
      "snippetIntents": [
        {
          "content": "<这部分代码的作用/内容>",
          "line": "<所在行号>",
          "associatedId": "<关联的函数或方法 Id，无关联则为 null>",
          "associatedType": "<关联的函数的方式, 可选值为 'call' | 'return' | 'indirect' | null>",
          "associatedRequired": "<是否和该功能强相关，可选值为 true | false>"
        }
      ]
    }
  ]
}
```

### 字段说明

- `projectRoot`：被分析项目的根目录**绝对路径**，用于渲染器详情面板读取源码
- `associatedType` 取值：
  - `call`：仅调用该关联函数
  - `return`：调用后使用其返回值
  - `indirect`：间接关联或调用（如通过请求路径/中间件间接触发）
  - `null`：无关联函数（`associatedId` 为 `null` 则 `associatedType` 也必须为 `null`）
- `associatedRequired`：
  - `true`：该关联与功能核心逻辑强相关
  - `false`：辅助性代码（打日志、鉴权、参数校验等对理解核心功能无益），且无关联函数时为 `false`（`associatedId` 为 `null` 则 `associatedRequired` 必须为 `false`）

## 分析方法

1. **确定入口**：用户给出的函数可能不是最上游，需要先通过调用关系找到最上游的入口函数/方法，比如某个挂载时调用的初始化方法或者 click 事件的处理函数等。
2. **逐层展开**：从入口出发，识别其内部调用、间接依赖、返回值使用等下游函数，逐个加入待分析队列。
3. **请求链路**：如果遇到请求方法且前后端项目都可见，则根据请求路径继续分析后端上游/下游。
4. **完整覆盖**：务必梳理出该功能的所有调用链路，不能遗漏。可结合 IDE 的引用查找与搜索功能辅助分析。
5. **逐轮推进**：调用链可能较长，不要一次性全部分析。每轮只针对一个函数/方法，边分析边把下游相关函数及位置收集进待分析数组，不断填充 JSON 结果数组，直到清空数组、完整梳理出该功能流程后再进行渲染。

## 渲染流程

分析完成后，执行以下步骤将结果渲染到浏览器（渲染器位于本 skill 目录下的 `trace-flow/` 项目）：

1. **写入数据文件**：将完整 JSON 以 UTF-8 编码写入 `trace-flow/data/<功能名>.json`。功能名使用小写英文与连字符（如 `homework-detail.json`），语义化地概括被分析的功能。

2. **启动渲染并打开浏览器**：在 `trace-flow` 目录下执行：

   ```bash
   node scripts/serve.mjs --data <功能名>.json
   ```

   脚本基于 Node 内置模块实现，**无需安装任何依赖**（仅需本机装有 Node.js 18+）；`dist/` 静态产物已随仓库提供。脚本会自动完成：启动/复用本地服务（默认端口 4173）、打开浏览器展示指定数据的调用链树图。

3. **异常处理**：
   - 若无法自动打开浏览器，向用户输出访问链接：`http://localhost:4173/?data=<功能名>.json`
   - 若脚本提示端口被占用，加 `--port <其他端口>` 重试
   - 若渲染器代码被更新过，加 `--rebuild` 重新构建（需要开发环境，即已执行过 `npm install`）

4. **告知用户**：渲染完成后，简要说明已在浏览器中打开调用链树图，并提示可点击节点查看函数详情与源码、使用搜索/折叠/「仅显示关键代码」等交互能力。

> 注意：`serve.mjs` 会以前台方式持有服务进程。AI 环境下应将脚本放到后台运行（不要等待其退出）。

## 示例

`examples` 是简化的模拟真实项目的目录，包含前端和后端的代码，用于演示分析过程和结果。

### 输入

```
梳理 "展示作业详情" 功能的代码逻辑
```

或给定代码函数片段：

```
分析
{
  "file_path": "./trace-flow/examples/frontend/detail.js",
  "line": "#L6-L17",
  "content": "async function loadDetail() { ... }"
}
的调用链路
```

### 输出

写入 `trace-flow/data/<功能名>.json`，结果示例：[data.json](./examples/output/data.json)

## 注意

- 数据文件内容必须是合法的 JSON，不要包含 JSON 之外的说明文字；也不要把分析过程输出到对话中。
- 每个 `snippetIntents` 中的条目都应与功能理解相关；辅助性函数（鉴权、校验、日志等）标记 `associatedRequired: false`。
- 每个 `id` 必须是唯一的，且为字符串类型，从 '1' 开始递增，`associatedId` 的值必须为已存在的 `id`值 或为 null。
- 当一行代码同时调用多个函数时，应为每个被调函数新增一个独立的 `snippetIntents` 条目（行号相同），分别描述并关联对应的 `associatedId`。
- 行号确保精确
- `projectRoot` 必须是被分析项目的绝对路径，否则详情面板无法读取源码
